ktor:
    application:
        modules:
            - com.syntex_error.ApplicationKt.module
    deployment:
        port: 7001
postgres:
  url: "jdbc:postgresql://localhost/default"
  user: username
  password:
  passwordjwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
